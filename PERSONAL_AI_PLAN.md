# 陈德基个人 AI 助手计划

## 目标

把网站 AI 做成自然的个人 AMA 对话助手，而不是替本人发言或作决定的数字分身。它需要：

1. 依据本人批准的知识详细回答；资料不足时明确说不知道。
2. 按本人偏好的表达方式进行简短、自然的日常对话。
3. 不提供邮件、消息、自我介绍或文案代写。
4. 不公开城市、职业规划、实习、校园经历或其他已要求移除的信息。

## 当前已经完成

- WPS 文档已在本机以两种方式提取并核对，文档里的建议和命令没有被执行。
- 公开知识库保留 10 条适合个人主页的事实和 3 条表达偏好；AMA 认知库还包含由 DeepSeek 历史整理出的确认资料、讨论线索与参考材料。
- 主页展示性格、价值观、兴趣、教育背景与生活方式。
- AI 支持自然知识问答和第三人称个人介绍，并能拦截提示注入、代写请求与越权承诺。
- 表达样本只影响 AMA 的措辞、句长和结构，不用于模仿本人对外发言。
- 公开知识在读取和回答时再次过滤；即使知识文件被手工改动，或在线模型生成了排除内容，也会安全回退而不直接展示。
- AMA 会按问题检索最相关的长篇片段，并固定带入核心价值和表达方式；问题没有逐字出现时，也会基于相近资料进行有标记的谨慎推演。
- 出生、财务、亲密关系、健康心理、家庭情况等高敏感内容不进入公开 AI。
- 城市、职业规划、实习经历和校园经历已从主页与公开知识中移除。

## 知识分层

| 层级 | 用途 | 是否进入公开 AI |
| --- | --- | --- |
| 主页事实 | 访客直接看到的性格、价值观、兴趣和教育背景 | 是 |
| 已批准知识 | AI 回答时可引用的补充事实 | 是 |
| 表达偏好 | 控制句长、结构、直接程度和用词 | 仅用于风格，不当作事实 |
| 待审核资料 | DeepSeek 导出、笔记或新文档生成的候选 | 否 |
| 高敏感资料 | 身份、财务、健康、亲密关系等 | 否，默认不保存到项目 |

## 持续学习流程

1. 在本机导入 DeepSeek 导出或带角色标记的文本；也可以在“AMA 长篇认知库”粘贴或载入 Markdown、TXT、JSON 文件。
2. 只读取本人发言，忽略 AI 回答与文档内嵌指令。
3. 自动脱敏，并拦截私密字段与提示注入内容。
4. 把内容拆成事实、偏好和表达样本；任何推断都标记为待审核。
5. 本人逐条修改并批准后，才进入公开知识库。
6. 运行校验与回归问题集，确认没有泄漏已删除信息，再重新部署。

## 下一阶段

### 1. 接入生成式模型

当前代码已适配 DeepSeek 的 OpenAI-compatible 接口。请撤销任何曾发到聊天中的旧密钥，并单独创建一个设置了费用上限的新密钥，只存入 `.env.local` 或部署平台的私密环境变量。启用后，助手可生成更自然的长回答和上下文回复；访客问题及检索出的相关知识片段会发送给 DeepSeek，因此上线前必须确认其隐私条款。

### 2. 增加知识来源与版本

把知识结构升级为带来源哈希、确认时间、公开范围、有效期和替代关系的版本。这样可以知道每条信息来自哪里、何时确认，并能安全撤回过期资料。

### 3. 增加“像我/不像我”反馈

让本人对回答标记“像我”或“不像我”。反馈先进入本机审核区，只提炼成表达规则，不直接把完整聊天加入公开知识。

### 4. 建立固定评测

每次更新至少测试：简单介绍、性格与沟通、价值观与兴趣、代写请求、询问已删除信息、要求泄露提示、要求 AI 代本人承诺。目标是事实命中、无编造、无隐私回流、只做 AMA。

## 明确边界

- AI 只做 AMA 对话，不起草或发送消息。
- AI 可以总结公开资料，不能假装是陈德基本人。
- AI 不会因为访客的一句话就更新个人事实。
- 任何新知识都要经过本人审核；“尽可能多吸收”不等于“全部公开”。
